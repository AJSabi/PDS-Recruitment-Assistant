name: Security Nuxt Family Preview

on:
  workflow_dispatch:
  push:
    branches:
      - feature/pds-skill-matrix
    paths:
      - .github/workflows/security-nuxt-family-preview.yml

permissions:
  contents: read

jobs:
  preview:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      DATABASE_URL: postgresql://validation:validation@127.0.0.1:5432/validation
      BETTER_AUTH_SECRET: validation-only-secret-not-used-at-runtime
      BETTER_AUTH_URL: http://localhost:3000
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Use npm 11 resolver
        run: |
          npm install --global npm@11.6.0
          npm --version

      - name: Generate pinned Nuxt-family candidate
        shell: bash
        run: |
          npm pkg set dependencies.nuxt=4.5.2
          npm pkg set 'overrides.@nuxt/devtools=3.4.1'
          npm pkg set 'overrides.@nuxt/vite-builder=4.5.2'
          npm pkg set 'overrides.@nuxt/nitro-server=4.5.2'
          npm pkg set 'overrides.@nuxt/kit=4.5.2'
          npm pkg set 'overrides.@nuxt/schema=4.5.2'
          npm install --package-lock-only --ignore-scripts

      - name: Install candidate graph
        run: npm ci --include=dev

      - name: Inspect resolved Nuxt family
        run: npm ls nuxt @nuxt/devtools @nuxt/vite-builder @nuxt/nitro-server @nuxt/kit @nuxt/schema rolldown --all || true

      - name: Audit candidate
        shell: bash
        run: |
          npm audit --json > npm-audit.json || true
          node <<'NODE'
          const fs = require('node:fs')
          const report = JSON.parse(fs.readFileSync('npm-audit.json', 'utf8'))
          const counts = report.metadata?.vulnerabilities ?? {}
          console.log(`Audit counts: critical=${counts.critical ?? 0}, high=${counts.high ?? 0}, moderate=${counts.moderate ?? 0}, low=${counts.low ?? 0}, total=${counts.total ?? 0}`)
          for (const [name, item] of Object.entries(report.vulnerabilities ?? {})) {
            console.log(`[${String(item.severity).toUpperCase()}] ${name} | ${item.isDirect ? 'direct' : 'transitive'} | range=${item.range}`)
          }
          if ((counts.critical ?? 0) > 0) {
            console.error('Critical vulnerabilities remain in Nuxt-family candidate.')
            process.exit(1)
          }
          NODE

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Production build
        env:
          NODE_ENV: production
        run: npm run build

      - name: Upload candidate manifest and lockfile
        uses: actions/upload-artifact@v4
        with:
          name: nuxt-family-security-candidate
          path: |
            package.json
            package-lock.json
          if-no-files-found: error
